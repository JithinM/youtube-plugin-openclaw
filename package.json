{
  "name": "@vyamika/youtube-plugin-oc",
  "version": "1.0.4",
  "description": "YouTube plugin for OpenClaw – search channels, get transcripts, trending videos, and topic-based search",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/copy-plugin-json.cjs",
    "prepublishOnly": "npm version patch --no-git-tag-version && node scripts/sync-plugin-version.cjs && npm run build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "openclaw",
    "youtube",
    "plugin"
  ],
  "license": "MIT",
  "dependencies": {
    "@sinclair/typebox": "^0.34.0",
    "youtubei.js": "^16.0.1"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "dotenv": "^17.2.4",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
